/*
	De início criei um arquivo para servir de classe para o objeto que vou tratar, em seguida defini seus atributos e por último iniciei os métodos (show, pIn, pOut, abastecer e andar).
	O método show() retorna uma string e é ele que mostrará o valor dos atributos definidos na classe (km, gas e p). O método pIn() serve para adicionar uma pessoa ao carro, fazendo a verificação dentro do próprio método para checar se há a capacidade máxima de pessoas ou não. Já o método pOut() serve para remover uma pessoa, verificando se não há ninguém no carro. O método abastecer() recebe uma variável do tipo number, checa se já não tem a capacidade máxima de combustível no carro, se não tiver, ele irá checar se somando a variável que ele recebe com a quantidade atual de combustível não ultrapassará do valor máximo (10), caso passe, ele atribui a variável para 10, caso não, ele soma a quantidade atual com o valor da variável. Por último, o método andar() faz com que o carro ande, para isso, ele irá verificar se há pelo menos 1 litro de gasolina e 1 pessoa, caso tenha, a gasolina irá subtrair 1 e a quilometragem irá aumentar o seu valor atual mais 10. Caso não tenha pessoas ou gasolina, o programa irá avisar que não será possível realizar a viagem.
	Para chamar todos esses métodos, criei um arquivo para servir de main do programa. Nele crio um novo objeto do tipo Carro, logo após tem um do-while, para rodar o programa fazendo o menu criado ficar em um loop, até que a escolha seja “Acabar o role”. Dentro do do-while tem um menu com as opções e um switch-case para definir o que cada opção irá fazer. 

Fiz o trabalho sozinha. O aprendizado se resumiu em apenas alguns detalhes da linguagem typescript, o que sei fazer foi falado nos parágrafos acima. Não tive dificuldade realizando este trabalho. Levei em cerca de 30 minutos.

*/
